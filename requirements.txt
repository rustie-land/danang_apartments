# Da Nang Apartments — Python data pipeline
# Install: pip install -r requirements.txt  (or: uv pip install -r requirements.txt)

# Telegram parsing pipeline (parser.py)
telethon>=1.34,<2.0
supabase>=2.0,<3.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0

# CSV converter pipeline (converter.py)
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0            # needed by pandas.read_csv for .xlsx sources
deep-translator>=1.11,<2.0
